# US-07: Thêm sản phẩm vào giỏ hàng từ phòng thử đồ

## Bối cảnh
- Epic: Mua sắm & Giỏ hàng
- Vai trò: Người mua (User đã đăng nhập)
- Mục tiêu: Cho phép user thêm sản phẩm đang xem thử vào giỏ hàng một cách nhanh chóng, không cần rời khỏi trải nghiệm thử đồ.

## Phạm vi
- Hiển thị nút "Thêm vào giỏ" trên trang thử đồ (`/fitting-room`)
- Gọi API thêm sản phẩm vào giỏ
- Hiển thị feedback (toast notification) khi thêm thành công/thất bại
- Điều hướng đến trang giỏ hàng nếu user muốn

## Luồng chính (happy path)
- User đang xem ảnh mình mặc sản phẩm A trong phòng thử đồ
- User thấy nút "Thêm vào giỏ" ngay dưới phần giá sản phẩm
- User bấm nút → Nút hiện trạng thái Loading
- Client gọi API `POST /api/cart` với `{ product_id, quantity: 1 }`
- Server thêm vào bảng `cart_items` (hoặc cộng dồn nếu đã có)
- API trả về thành công → Hiển thị toast "🎉 Đã thêm vào giỏ hàng!"
- User có thể bấm "Xem giỏ hàng" để điều hướng đến `/cart`

## Luồng lỗi cần xử lý
- User chưa đăng nhập: Redirect về trang login (đã xử lý bởi ProtectedRoute)
- Sản phẩm không tồn tại: Hiển thị toast lỗi "Sản phẩm không tồn tại"
- Lỗi server: Hiển thị toast "Không thể thêm vào giỏ. Vui lòng thử lại"
- User spam click: Disable nút trong lúc đang loading

## Dữ liệu & validate gợi ý
- Input: `product_id` (lấy từ sản phẩm đang xem), `quantity` (mặc định: 1)
- Output: `{ success: true, cartItem: {...} }`
- Quantity phải > 0

## Acceptance Criteria
- Nút "Thêm vào giỏ" hiển thị rõ ràng ngay dưới giá sản phẩm
- Nút có gradient đẹp mắt và animation hover
- Loading state hiển thị spinner khi đang xử lý
- Toast notification xuất hiện 3 giây rồi tự ẩn
- Có nút "Xem giỏ hàng" để điều hướng nhanh

## Phi chức năng
- UX: Nút phải nổi bật để khuyến khích mua hàng (màu sắc tươi sáng)
- Performance: API phản hồi nhanh (< 500ms)
- Accessibility: Nút có aria-label phù hợp
